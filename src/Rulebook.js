class Rulebook {
    static isValidMove(piece, color, from, to) {
        console.log(`Analyzing move of ${color} ${piece} from ${from} to ${to}`);

        return true;
    }
}

export default Rulebook;